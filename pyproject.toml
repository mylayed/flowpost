[project]
name = "flowpost"
version = "1.0.0"
description = "FlowPost — Telegram autoposting bot (SaaS)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
